<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="App1_NossoChat.Views.CadastrarChatPage"
             BackgroundColor="{StaticResource Verdeapp}">
    <ContentPage.Content>
        <StackLayout>
            <Entry Text="{Binding Nome}" Placeholder="Digite o nome do chat" PlaceholderColor="{StaticResource Verdeapp}" BackgroundColor="White" TextColor="{StaticResource Verdeapp}"/>
            <Button Text="Cadastrar" Margin="0,20,10,0" HorizontalOptions="End" VerticalOptions="End" FontAttributes="Bold" Command="{Binding CadastrarCommand}" />
            <Label Text="{Binding Mensagem}" TextColor="White" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>